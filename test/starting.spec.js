const assert = require('assert');

describe('Basic Mocha Test', () => {
  it('Should throw errors', () => {
    // try {
    //   assert.equal(2,3);
    // } catch (e) {
    //   console.log(e);
    // };
  });
});